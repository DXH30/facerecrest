version: '3'

networks:
    facerec:

services:
    facer:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: face_recognition
        volumes:
            - .:/app
        ports:
            - "8012:3000"
        networks:
            - facerec
        command: sh -c initialize.sh
